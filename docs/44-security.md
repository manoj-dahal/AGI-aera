# 44. Security
## Hardened Execution
- **Sandboxing**: Plugins run in an isolated V8 context without access to `fs` or `process.env`.
- **Encryption**: Memories and credentials are AES-256 encrypted at rest.

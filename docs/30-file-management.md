# 30. File Management
## Semantic File System
AERA treats the file system semantically.

- **Organization**: Can automatically categorize, rename, and move files based on content.
- **Search**: Uses vector search to find documents based on meaning, not just filenames.
